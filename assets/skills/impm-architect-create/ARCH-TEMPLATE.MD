# 架构文档

**项目中文名称：** {PROJECT_NAME_CN}
**项目名称：** {PROJECT_NAME}
**版本号：** v{X.Y.Z}
**日期：** {YYYY-MM-DD}


---

## 1. 系统架构概述

### 1.1 系统定位
{一句话描述系统是什么、解决什么问题、在业务版图中的位置}

### 1.2 架构风格
- **选用风格：** {单体架构 / 微服务 / 分层架构 / 事件驱动 / 插件化 / 主从编排 / 其他}
- **选型理由：** {结合 PRD 中的非功能性需求说明为何选用此风格}

### 1.3 架构层次图
```
{多层次结构，文本符号绘图}
```
### 1.4 核心架构特点

| 特点 | 说明 |
|------|------|
| {特点1} | {一句话解释} |
| {特点2} | {一句话解释} |
| {特点3} | {一句话解释} |

---

## 2. 模块设计

### 模块1：{模块名称}（{模块英文标识}）

- **职责：** {该模块负责什么，不做什么（明确边界）}
- **依赖：** {依赖哪些其他模块或外部服务}
- **接口：** {对外暴露的主要接口或函数签名}
- **说明：** {补充说明，如运行模式、生命周期等}

### 模块2：{模块名称}（{模块英文标识}）

- **职责：** {职责描述}
- **依赖：** {依赖列表}
- **接口：** {接口定义}
- **说明：** {补充说明}

### 模块关系图


---

## 3. 技术选型

### 3.1 技术栈全景

| 领域 | 技术方案 | 版本要求 | 选型理由 |
|------|---------|---------|---------|
| 编程语言 | {语言} | {版本} | {理由} |
| 运行环境 | {运行时} | {版本} | {理由} |
| 模块系统 | {ESM/CJS/其他} | - | {理由} |
| 框架/平台 | {框架名} | {版本} | {理由} |
| 数据存储 | {数据库/文件系统} | {版本} | {理由} |
| 缓存方案 | {Redis/内存/其他} | {版本} | {理由} |
| 测试框架 | {测试工具} | {版本} | {理由} |
| 文档格式 | {Markdown/JSON/其他} | - | {理由} |
| 版本号规范 | {SemVer/其他} | - | {理由} |
| 部署方式 | {容器化/无服务器/插件/其他} | - | {理由} |

### 3.2 架构决策记录（ADR）

| ADR编号 | 决策内容 | 选项对比 | 最终选择 | 理由 | 后果/权衡 |
|---------|---------|---------|---------|------|----------|
| ADR-001 | {决策主题} | {选项A} / {选项B} | {选项A} | {核心原因} | {引入的复杂度或限制} |
| ADR-002 | {决策主题} | {选项A} / {选项B} | {选项B} | {核心原因} | {引入的复杂度或限制} |

---

## 4. 数据流

### 4.1 核心业务数据流


### 4.2 模块间数据流转


### 4.3 数据存储流转

| 数据类型 | 产生阶段 | 存储位置 | 消费阶段 | 生命周期 |
|---------|---------|---------|---------|---------|
| {数据类型A} | {阶段X} | {路径/数据库表} | {阶段Y} | {临时/持久} |
| {数据类型B} | {阶段X} | {路径/数据库表} | {阶段Y} | {临时/持久} |

---

## 5. 数据架构

### 5.1 数据模型概览

{核心实体关系描述，或插入 ER 图/简化关系图}

### 5.2 存储策略

| 数据类型 | 存储方案 | 理由 | 一致性要求 |
|---------|---------|------|-----------|
| {结构化数据} | {关系型数据库} | {理由} | {强一致性/最终一致性} |
| {非结构化数据} | {对象存储/文件系统} | {理由} | {最终一致性} |
| {临时数据} | {内存/缓存} | {理由} | {无} |
| {日志数据} | {时序数据库/文件} | {理由} | {无} |

### 5.3 数据一致性策略
- {策略1：如 ACID 事务}
- {策略2：如 Saga 补偿事务}
- {策略3：如 事件溯源}

---

## 6. 接口设计

### 6.1 外部接口

| 接口名称 | 协议 | 路径/主题 | 认证方式 | 调用方 |
|---------|------|----------|---------|--------|
| {接口A} | {REST/gRPC/WebSocket/MQ} | {路径} | {JWT/OAuth/无} | {调用方} |
| {接口B} | {REST/gRPC/WebSocket/MQ} | {路径} | {JWT/OAuth/无} | {调用方} |

### 6.2 内部接口

| 接口名称 | 通信方式 | 数据格式 | 调用关系 |
|---------|---------|---------|---------|
| {内部接口A} | {函数调用/IPC/消息队列} | {JSON/Protobuf/其他} | {模块X → 模块Y} |

### 6.3 接口契约示例

```typescript
// {接口名称} 请求/响应定义
interface {RequestName} {
  {field}: {type};  // {描述}
}

interface {ResponseName} {
  {field}: {type};  // {描述}
} 
```

## 7. 非功能性需求落地（NFR Implementation）
将 PRD 中的非功能性需求转化为具体技术决策：
| PRD-NFR编号 | 非功能性需求   | 架构决策          | 量化指标           |
| --------- | -------- | ------------- | -------------- |
| {NFR-001} | {性能要求}   | {缓存层/异步处理}    | {响应时间 < 200ms} |
| {NFR-002} | {可用性要求}  | {多可用区/故障转移}   | {SLA 99.9%}    |
| {NFR-003} | {可扩展性要求} | {无状态设计/水平扩展}  | {支持 10x 流量增长}  |
| {NFR-004} | {安全性要求}  | {TLS/加密/RBAC} | {通过安全审计}       |


## 8. 部署与运维视图
### 8.1 部署架构
```{用户/客户端}
   │
   ▼
{接入层（CDN/LB/Gateway）}
   │
   ▼
{服务层（容器/实例/插件）}
   │
   ├─→ {数据层（数据库/缓存）}
   └─→ {存储层（文件/对象存储）}
```   
### 8.2 环境划分
| 环境    | 用途   | 部署方式    | 数据策略     |
| ----- | ---- | ------- | -------- |
| 开发环境  | {用途} | {本地/容器} | {隔离/共享}  |
| 测试环境  | {用途} | {容器/CI} | {隔离/重置}  |
| 预发布环境 | {用途} | {生产镜像}  | {生产数据子集} |
| 生产环境  | {用途} | {生产部署}  | {生产数据}   |

### 8.3 可观测性
| 类型   | 方案                      | 采集内容          |
| ---- | ----------------------- | ------------- |
| 日志   | {ELK/Loki/文件}           | {业务日志/错误日志}   |
| 指标   | {Prometheus/CloudWatch} | {QPS/延迟/错误率}  |
| 链路追踪 | {Jaeger/Zipkin}         | {请求全链路}       |
| 告警   | {PagerDuty/钉钉}          | {SLA 违反/错误阈值} |

### 8.4 灾难恢复
| 场景    | RPO  | RTO  | 恢复策略      |
| ----- | ---- | ---- | --------- |
| {场景A} | {时间} | {时间} | {自动/手动切换} |
| {场景B} | {时间} | {时间} | {备份恢复}    |

## 9. 安全架构
### 9.1 安全分层
| 层次  | 防护措施   | 实现方式                 |
| --- | ------ | -------------------- |
| 传输层 | {加密传输} | {TLS 1.3/mTLS}       |
| 认证层 | {身份验证} | {JWT/OAuth2/API Key} |
| 授权层 | {权限控制} | {RBAC/ABAC}          |
| 数据层 | {数据保护} | {加密存储/脱敏}            |
| 审计层 | {操作审计} | {日志记录/不可篡改}          |

### 9.2 敏感数据处理
| 数据类型     | 处理方式    | 存储位置       |
| -------- | ------- | ---------- |
| {密码/密钥}  | {哈希/加密} | {KMS/环境变量} |
| {个人身份信息} | {脱敏/加密} | {加密数据库}    |


## 10. 目录结构
```lc
{project-root}/
├── {配置目录}/                        # {用途说明}
│   ├── {agents/}                      # {Agent 配置目录}
│   │   └── {agent-config}.md          # {具体配置}
│   └── {skills/}                      # {技能实现目录}
│       └── {skill-name}/
│           └── SKILL.md               # {技能定义}
├── docs/                              # 文档目录
│   ├── project.md                     # 项目信息文档
│   ├── architecture.md                # 架构文档（本文件）
│   ├── requires/                      # 需求文档
│   ├── prds/                          # PRD 文档
│   ├── sds/                         # 技术规格
│   └── tasks/                         # 任务清单
├── src/                               # 源代码
│   ├── index.ts                       # 入口文件
│   ├── {模块A}/                       # {模块说明}
│   └── {模块B}/                       # {模块说明}
├── __tests__/                         # 测试目录（与 src 结构对应）
├── package.json                       # 项目配置
├── tsconfig.json                      # TypeScript 配置
└── README.md                          # 项目自述
```
## 11. 架构质量属性
| 质量属性     | 实现策略              | 验证方式        |
| -------- | ----------------- | ----------- |
| **可维护性** | {分层/模块化/单一职责}     | {代码审查/圈复杂度} |
| **可测试性** | {纯函数优先/依赖注入/接口隔离} | {单元测试覆盖率}   |
| **可扩展性** | {插件化/配置化/水平扩展}    | {新增模块成本}    |
| **可靠性**  | {容错/重试/降级/熔断}     | {混沌测试/故障注入} |
| **可移植性** | {容器化/无状态/标准协议}    | {跨环境部署}     |
| **性能**   | {缓存/异步/批处理/索引}    | {压力测试/基准测试} |
| **安全性**  | {最小权限/加密/审计}      | {安全扫描/渗透测试} |

## 12. 附录
### A. 接口规范模板
``` js
// {模块/工具} 接口定义
interface {InterfaceName} {
  // {方法描述}
  {methodName}(args: {ArgsType}): Promise<{ReturnType}>;
}

// 参数 Schema
interface {ParameterSchema} {
  type: '{string|number|boolean|object|array}';
  description: string;
  required?: boolean;
  enum?: string[];
}
```
### B. 文档命名规范
| 文档类型  | 目录   | 文件名格式    | 示例     |
| ----- | ---- | -------- | ------ |
| {类型A} | {路径} | `{命名规则}` | `{示例}` |
| {类型B} | {路径} | `{命名规则}` | `{示例}` |

### C. 版本号规则
格式： v{主版本}.{次版本}.{修订号}
递增规则：
主版本：{何时升级}
次版本：{何时升级}
修订号：{何时升级}

### D. 参考文档
{PRD 文档链接/路径}
{技术规格链接/路径}
{外部参考文档链接}