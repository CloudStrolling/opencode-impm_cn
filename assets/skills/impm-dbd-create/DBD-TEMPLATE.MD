# 数据库设计文档
---
**项目中文名称：** {PROJECT_NAME_CN}
**项目名称：** {PROJECT_NAME}
**版本号：** v{X.Y.Z}
**日期：** {YYYY-MM-DD}

---

# 1. 项目数据库选型
- **数据库**：{数据库类型及版本}
- **字符集**：{字符集}
- **时区**：{时区（建议应用层统一处理）}
- **存储引擎**：{存储引擎}
- **分片策略**：{分片策略描述（如无分片则填"无"）}

---

# 2. 命名规范

| 对象 | 规则 | 示例 |
|------|------|------|
| 库名 | `项目缩写_业务域` | `ec_order`, `ec_payment` |
| 表名 | 小写 + 下划线，单数名词 | `order`, `order_item` |
| 字段名 | 小写 + 下划线，动词/名词 | `created_at`, `is_deleted` |
| 主键 | `pk_表名` | `pk_order` |
| 唯一索引 | `uk_表名_字段` | `uk_order_order_no` |
| 普通索引 | `idx_表名_字段` | `idx_order_user_id_status` |
| 分区表后缀 | `_p` | `order_p` |

---

# 3. 物理模型

## 3.1 {表名}（{table_name}）

| 字段名 | 类型 | 长度/精度 | 可空 | 默认值 | 约束 | 说明 |
|--------|------|----------|------|--------|------|------|
| {field} | {type} | {length} | {YES/NO} | {default} | {PK/UK/None} | {description} |

**索引设计**：
- `{index_name}` ({field}) —— {用途说明}

**分片策略**：{不分片 / 分片策略说明}

---

# 4. 数据字典（枚举值汇总）

| 字段 | 枚举值 | 含义 |
|------|--------|------|
| {table.field} | {value} | {description} |

---

# 5. 安全与权限

| 角色 | 库/表权限 | 说明 |
|------|----------|------|
| {role} | {permissions} | {description} |

**敏感字段脱敏**：
- `{table.field}`：{脱敏规则}

---

# 6. 备份与容灾

| 策略 | 配置 |
|------|------|
| {备份类型} | {策略描述} |
